---
const pathname = Astro.url.pathname;
const isBlog = pathname === "/blog" || pathname.startsWith("/blog/");
---

<header class="border-b border-border-subtle/80 bg-bg/80 backdrop-blur-sm">
  <nav class="max-w-3xl mx-auto px-6 h-14 flex items-center justify-between">
    <a
      href="/"
      class="inline-flex items-center gap-2 text-sm font-semibold tracking-tight text-text no-underline"
    >
      <span
        class="inline-flex h-5 w-5 items-center justify-center rounded-md bg-accent-soft text-[11px] font-mono text-accent"
      >
        UA
      </span>
      <span>Uzair Aftab</span>
    </a>

    <div class="flex items-center gap-6">
      <a
        href="/blog"
        class:list={[
          "text-sm no-underline transition-colors",
          isBlog ? "text-text" : "text-text-3 hover:text-text"
        ]}
      >
        Blog
      </a>
    </div>
  </nav>
</header>
